#include <stdio.h>
#include "Add.h"
void main()
{
int n,i;
double arr[100];
printf("Enter the number of elements in the array : ");
scanf("%d",&n);
printf("\nEnter the elements of the array : \n");
for(i = 0 ; i < n ; i++)
scanf("%lf",&arr[i]);
addarr(n,arr);
}